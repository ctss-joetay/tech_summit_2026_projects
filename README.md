# Tech Summit 2026 — Student Projects

Each group's project lives in `groups/<gid>/`. All commits are made by
the platform bot; students publish through the Summit IDE.

`groups/<gid>/.summit/` holds platform metadata (AI context summaries,
project brief). It is committed but never served.
