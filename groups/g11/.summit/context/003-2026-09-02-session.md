---
id: 003
date: 2026-09-02
topic: session
files_touched: 
decisions:
  - none
open_questions:
  - none
---

I'll build this as a self-contained simulation page with:

1. **Top area split into two side-by-side boxes**: left = CLI input/output, right = live logs/stats.
2. **CLI command** `rate <value>` (or presets like `rate peak`, `rate quiet`) to change the arrival/flow rate on the fly, simulating peak hours.
3. A simple flow simulator running underneath that generates "arrivals" on an interval controlled by that rate, logging each event to the right-hand log box.

```html
<!DOCTYPE html>
<html lang="en">
<head>
<meta charset="UTF-8">
<title>Flow Simulator</title>
<style>
  body {
    font-family: monospace;
    background: #111;
    color: #eee;
    margin: 0;
    padding: 20px;
  }

  h1 {
    font-size: 18px;
    margin-bottom: 16px;
  }

  .top-container {
    display: flex;
    gap: 16px;
    margin-bottom: 20px;
  }

  .box {
    flex: 1;
    background: #1b1b1b;
    border: 1px solid #333;
    border-radius: 6px;
    padding: 12px;
    display: flex;
    flex-direction: column;
    height: 260px;
  }

  .box h2 {
    font-size: 13px;
    color: #8ab4f8;
    margin: 0 0 8px 0;
    text-transform: uppercase;
    letter-spacing: 1px;
  }

  /* CLI box */
  #cli-output {
    flex: 1;
    overflow-y: auto;
    font-size: 13px;
    line-height: 1.4;
    white-space: pre-wrap;
    margin-bottom: 8px;
  }

  #cli-input-row {
    display: flex;
    gap: 6px;
  }

  #cli-input {
    flex: 1;
    background: #000;
    color: #0f0;
    border: 1px solid #333;
    padding: 6px 8px;
    font-family: monospace;
    font-size: 13px;
  }

  #cli-input:focus {
    outline: 1px solid #0f0;
  }

  /* Logs box */
  #log-output {
    flex: 1;
    overflow-y: auto;
    font-size: 12px;
    line-height: 1.4;
    color: #aaa;
  }

  .log-line {
    border-bottom: 1px solid #222;
    padding: 2px 0;
  }

  .status-line {
    font-size: 12px;
    color: #999;
    margin-top: 4px;
  }

  .rate-tag {
    color: #f5c542;
    font-weight: bold;
  }
</style>
</head>
<body>

<h1>Flow Simulator</h1>

<div class="top-container">
  <!-- CLI box -->
  <div class="box" id="cli-box">
    <h2>CLI</h2>
    <div id="cli-output"></div>
    <div id="cli-input-row">
      <span>&gt;</span>
      <input id="cli-input" type="text" placeholder="type: rate peak | rate quiet | rate 500 | help" autofocus>
    </div>
    <div class="status-line">Current rate: <span class="rate-tag" id="rate-display">1000ms</span></div>
  </div>

  <!-- Logs box -->
  <div class="box" id="log-box">
    <h2>Live Log</h2>
    <div id="log-output"></div>
  </div>
</div>

<script>
(() => {
  const cliOutput = document.getElementById('cli-output');
  const cliInput = document.getElementById('cli-input');
  const logOutput = document.getElementById('log-output');
  const rateDisplay = document.getElementById('rate-display');

  // Rate presets in ms between arrivals (lower = busier)
  const RATE_PRESETS = {
    peak: 200,     // fast arrivals, simulate rush hour
    normal: 1000,  // baseline
    quiet:
